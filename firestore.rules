rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 駐車場データは合言葉(yardId)ごと。匿名認証済みの端末のみ読み書き可。
    match /yards/{yardId} {
      allow read, write: if request.auth != null;
    }
  }
}
